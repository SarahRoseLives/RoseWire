import 'package:flutter/material.dart';

class AboutPanelMobile extends StatelessWidget {
  const AboutPanelMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("AboutPanel (Android UI)"));
  }
}